import { PARAMETER_TYPE } from '../../utils/parameter-type'

export default {
    evenAngles: {
        type: PARAMETER_TYPE.BOOL,
        range: [true, false],
        default: false
    }
}
